# terraform providers

registry.terraform.io/hashcorp/local

```hcl
resource "random_pet" "my-pet" {
  prefix = "Mrs"
  spearator = "."
  length = "1"
}

resource "random_string" "iac_random" {
  length = 10
  min_upper = 5
}


```

